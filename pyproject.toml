[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 88
exclude = ["python/"]

[tool.ruff.format]
indent-style = "space"
quote-style = "double"   # or "single"
